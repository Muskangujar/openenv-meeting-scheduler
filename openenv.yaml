name: meeting-scheduler-env
version: 1.0.0
description: Real-world meeting scheduling environment with constraints and preferences

entrypoint: server:app

observation_space: Observation
action_space: Action
reward: float

tasks:
  - easy_single_slot
  - medium_conflicts
  - hard_preferences