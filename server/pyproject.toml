[project]
name = "meeting-scheduler-env"
version = "1.0.0"
description = "OpenEnv meeting scheduler environment"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv-core>=0.2.0"
]
[project.scripts]
server = "server:main"